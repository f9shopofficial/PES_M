ขั้นตอนการเอาไปใช้

run คำสั่งนี้ที่ terminals

pip install -r requirements.txt

=======================================================

ถ้าอยาก commit ให้ run คำสั่งนี้ที่ terminals

git config --global user.name "F9Share"
git config --global user.email "f9share@gmail.com"

git config --global user.name "Lubpui"
git config --global user.email "yukito3644@gmail.com"

git config --global user.name "sparkbi36"
git config --global user.email "sparkbi36@gmail.com"

git config --global user.name "watcharapol-chue"
git config --global user.email "watcharapol.chue@gmail.com"

git config --global user.name "ankomaboo1"
git config --global user.email "ankomaboo1@gmail.com"

git config --global user.name
git config --global user.email